<Window x:Class="KinectMotionCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KinectMotionCapture" Height="600" Width="800"
        Closing="MainWindow_Closing">
    <Window.Resources>
        <SolidColorBrush x:Key="MediumGreyBrush" Color="#ff6e6e6e" />
        <SolidColorBrush x:Key="KinectPurpleBrush" Color="#ff52318f" />
        <SolidColorBrush x:Key="KinectBlueBrush" Color="#ff00BCF2" />
    </Window.Resources>
    <Border Background="WhiteSmoke" BorderBrush="Black" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Content="Record" Name="RecordButton" Height="23" Width="75" VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="RecordButton_Click" Grid.Column="0" Margin="10" />
            <Image Source="{Binding BackgroundImage}" Stretch="UniformToFill" Grid.Row="1" />
            <Image Name="BodyImage" Source="{Binding ImageSource}" Stretch="UniformToFill" Grid.Row="1" />
            <StatusBar Grid.Row="1" HorizontalAlignment="Stretch" Name="statusBar" VerticalAlignment="Bottom" Background="White" Foreground="{StaticResource MediumGreyBrush}">
                <StatusBarItem Content="{Binding StatusText}" />
            </StatusBar>
        </Grid>
    </Border>
</Window>
